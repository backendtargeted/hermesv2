services:
  hermes-app:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
      - ./Challenge FIles:/app/Challenge FIles
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
